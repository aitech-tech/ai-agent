# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
.venv/

# Storage — contains real auth tokens, logs, snapshots (never commit these)
storage/tokens.json
storage/agent.log
storage/health.json
storage/dashboard_snapshot.html

# Encrypted skills — generated by installer on client machine
skills/base/*.json.enc

# Credentials — never commit real credentials
# connectors.json is committed but contains only platform OAuth app credentials (safe to ship)
.env
*.env

# Build output
dist/
build/
*.egg-info/

# IDE
.vscode/
.idea/
*.suo
*.user

# OS
.DS_Store
Thumbs.db
desktop.ini
