"""
Products — Python pre-processing script framework for ReckLabs AI Agent.

Each sub-package (e.g. zoho_books) contains report scripts that fetch,
filter, and aggregate data locally, returning compact summaries to Claude.
Claude narrates the result; it does not reprocess raw records.
"""
