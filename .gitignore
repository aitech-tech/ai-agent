# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
.venv/

# Storage — contains real auth tokens, logs, snapshots (never commit these)
storage/tokens.json
storage/agent.log
storage/health.json
storage/dashboard_snapshot.html
storage/tmp/

# Encrypted skills — generated by installer on client machine
skills/base/*.json.enc

# Credentials — never commit real credentials
# .env holds the platform OAuth credentials; .env.example is the safe template (no secrets)
.env
!.env.example

# Build output
dist/
build/
*.egg-info/

# IDE
.vscode/
.idea/
*.suo
*.user

# OS
.DS_Store
Thumbs.db
desktop.ini
